---
import { Image } from "astro:assets";
import Button from "./Button.astro";
import HeaderImg from "../assets/Card.svg";
---

<section class="flex py-10 lg:mt-10">
  <div class="max-w-2xl pt-10 w-full">
    <h1
      class="capitalize text-textPrimary font-extrabold text-5xl sm:text-6xl text-balance leading-tight"
    >
      transaction control material made easier
    </h1>
    <p
      class="text-[#5A7184] text-lg my-4 xl:my-8 font-normal text-pretty max-w-xl leading-loose"
    >
      Cash Bank is constantly improving to move forward through transformation,
      innovation, data security, and superior human resources. Give meaning to a better
      life.
    </p>

    <div class="flex justify-start gap-5 pt-4 items-center">
      <Button title="Create Free Account" />
      <button
        class="rounded-xl py-3.5 px-3.5 sm:px-7 text-textPrimary font-bold text-opacity-80 text-sm sm:text-base"
      >
        Find Out More
      </button>
    </div>
  </div>
  <div class="relative hidden lg:block w-full aspect-square overflow-hidden">
    <Image
      src={HeaderImg}
      alt="header-info-cards"
      class="absolute inset-0 object-cover w-full h-full"
      quality={100}
      width={1200}
      height={1200}
      format="svg"
    />
  </div>
</section>
